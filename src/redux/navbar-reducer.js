const initialState = {
   friends: [
      { id: 1, name: 'Dimych' },
      { id: 2, name: 'Andrey' },
      { id: 3, name: 'Valera' }
   ]
}

const navbarReducer = (state = initialState, action) => {

   

   return state;
}

export default navbarReducer;